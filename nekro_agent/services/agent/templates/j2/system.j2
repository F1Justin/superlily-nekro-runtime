{% macro system_prompt(stable_static, channel_static, runtime_dynamic, plugins_prompt) %}
{{ stable_static }}

{{ channel_static }}

{{ runtime_dynamic }}

## Available Methods

<plugins>
{{ plugins_prompt }}
</plugins>

Methods marked `[AGENT METHOD - STOP AFTER CALL]` return information in a later iteration. After calling one, stop the current script immediately.

## Final Output Contract

Raw executable Python only. Begin with an actual Python statement; never begin with a language label, Markdown fence, or explanatory text.
{% endmacro %}
